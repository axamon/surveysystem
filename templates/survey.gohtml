<!DOCTYPE html>
<html>
<head>
<link rel="stylesheet" href="styles.css">
</head>
{{$name := .}}
<h1>{{.Titolo}}</h1>
<h2>Inizio: {{.Inizio}} Fine: {{.Fine}}</h2>
<h3>Ciao {{.Utente}},</h3>

<div>
<form method="POST" action="/survey">
<input type="hidden" id="surveyID" name="surveyID" value={{.ID}}>
<input type="hidden" id="utente" name="utente" value={{.Utente}}>
<input type="hidden" id="matricola" name="matricola" value={{.Matricola}}>
<ul>
{{range $index, $value := .Domande.Domanda}}

    {{ if (eq .Tipo "booleana") }}
        <li>{{.Text}}</li>
        <input type="radio" id="si" name={{.IDdomanda}} value="si">
        <label for="si">Si</label>
        <input type="radio" id="no" name={{.IDdomanda}} value="no">
        <label for="no">No</label>
        <br>
    {{end}}

    {{ if (eq .Tipo "libera") }}
        <li>{{.Text}}</li>
        <input type="textarea" id="textarea" name={{.IDdomanda}}>
    {{end}}

    {{ if (eq .Tipo "multipla") }}
        <li>{{.Text}}</li>
        <select name={{.IDdomanda}} id="elementi" multiple>
        {{range $indexr, $value := .Opzioni.Opzione}}
             <option value={{$value}}>{{$value}}</option>
        {{end}}
        </select>
    {{end}}

{{end}}
 <input type="submit" value="Invia">
</ul>
</form>
</div>
<div class="footer">Made by <a href="mailto:alberto.bregliano@telecomitalia.it">Alberto Bregliano</a> - Copyright 2020 &copy - All Rights Reserved</div>

</html>